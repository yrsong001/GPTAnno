% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_anno.R
\name{gptanno}
\alias{gptanno}
\title{Run Annotation Workflow for All Cluster Resolutions}
\usage{
gptanno(
  seurat_obj,
  resolutions,
  cl,
  graph,
  mapping_dict = GPTAnno::GPTCelltyp_mapping,
  model = "gpt-5",
  tissue_name = NULL,
  n_runs = 2,
  topgenenumber = 10,
  add_cl_prompt = FALSE,
  marker_dir = "output/marker_genes",
  save_plots = FALSE,
  plot_dir = "./output/prediction",
  llm_config = NULL
)
}
\arguments{
\item{seurat_obj}{A Seurat object.}

\item{resolutions}{Numeric vector of resolutions to annotate.}

\item{cl}{The ontology object.}

\item{graph}{An igraph object representing the Cell Ontology DAG. This can be created using:
\code{cl <- ontologyIndex::get_ontology("http://purl.obolibrary.org/obo/cl.obo", extract_tags = "everything")}
and \code{graph <- build_ontology_graph(cl)}.}

\item{mapping_dict}{Named character vector or data.frame; mapping from GPT-predicted to CL names. Defaults to package data \code{GPTCelltyp_mapping}.}

\item{model}{Character. Model to use (default: 'gpt-5').}

\item{tissue_name}{Character. Optional context for prompt.}

\item{n_runs}{Integer. Number of GPT calls to aggregate (default: 2).}

\item{topgenenumber}{Integer. Number of top marker genes per cluster to include (default: 10).
This parameter is passed to \code{summarize_gptcelltype()} and \code{gptcelltype()} to control
how many of the top marker genes are used for each cluster in the annotation process.}

\item{add_cl_prompt}{Logical. If TRUE, adds "Please try to predict cell types in Cell Ontology." to the prompt.}

\item{marker_dir}{Character. Directory containing marker gene files (default: "output/marker_genes").
Marker files should be named "markers_res_\if{html}{\out{<resolution>}}.rds".}

\item{save_plots}{Logical. Whether to save comparison plots (default: FALSE).}

\item{plot_dir}{Character. Directory to save plots (default: "./output/prediction").
Only used if save_plots = TRUE.}

\item{llm_config}{Optional list. LLM configuration passed to summarize_gptcelltype.
Supports OpenAI, Anthropic, and Google Gemini providers.}
}
\value{
A named list of annotation summary objects for each resolution.
}
\description{
Applies GPT annotation workflow for all specified clustering resolutions.
For each, retrieves markers, runs annotation, calculates ontology distances, and stores output.
}
