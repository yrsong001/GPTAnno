% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subcluster_anno.R
\name{run_subcluster_annotation_workflow}
\alias{run_subcluster_annotation_workflow}
\title{Run Complete Subcluster Annotation Workflow}
\usage{
run_subcluster_annotation_workflow(
  base_dir = "output/subclusters",
  strategy = c("ontology", "marker_inheritance"),
  cl = NULL,
  parent_marker_root = NULL,
  parent_res = "0.3",
  parent_cluster_col = "cluster_res.0.3",
  user_restrict_to = NULL,
  combine_restrictions = TRUE,
  model = "gpt-5",
  tissue_name = NULL,
  resolutions = c(0.1, 0.3, 0.5),
  n_runs = 2,
  topgenenumber = 10,
  add_cl_prompt = FALSE,
  ontology_graph = NULL,
  save_results = TRUE,
  select_best = TRUE,
  output_dir = NULL,
  celltypes_to_subcluster = NULL
)
}
\arguments{
\item{base_dir}{Directory with subcluster Seurat objects (each celltype in subdirectory)}

\item{strategy}{Character. "ontology" or "marker_inheritance"}

\item{cl}{Ontology object (required for both strategies). Used to calculate ontology distance
for scoring, and to get descendants for ontology strategy.}

\item{parent_marker_root}{Directory for parent markers (required for marker_inheritance)}

\item{parent_res}{Character. Resolution of parent clustering (for marker_inheritance)}

\item{parent_cluster_col}{Character. Column name for parent clusters in subset metadata}

\item{user_restrict_to}{Named list or character vector. Cell type restrictions.
Can be: NULL, character vector (applied to all), or named list per celltype}

\item{combine_restrictions}{Logical. For ontology strategy, combine user + descendants?}

\item{model}{Character. GPT model (default: "gpt-5")}

\item{tissue_name}{Character. Tissue context}

\item{resolutions}{Numeric vector. Resolutions to annotate}

\item{n_runs}{Integer. Number of GPT runs (default: 2)}

\item{topgenenumber}{Integer. Top genes per cluster (default: 10)}

\item{add_cl_prompt}{Logical. Add CL prompt (default: FALSE)}

\item{ontology_graph}{Optional igraph for distance calculation. If NULL and cl is provided,
the graph will be automatically built using build_ontology_graph(cl)}

\item{save_results}{Logical. Save intermediate results? (default: TRUE)}

\item{select_best}{Logical. Auto-select best resolution using score_annotation_resolutions()? (default: TRUE)}

\item{output_dir}{Optional directory for outputs}

\item{celltypes_to_subcluster}{Optional character vector. Specific parent celltypes to process.
If NULL, processes all directories in base_dir. Overrides any celltype filtering.}
}
\value{
List with: results (all resolutions for all celltypes), scores, best, strategy
}
\description{
Master function that orchestrates the entire subcluster annotation process
for both ontology and marker-inheritance strategies.
}
\examples{
\dontrun{
# Ontology strategy
workflow_results <- run_subcluster_annotation_workflow(
  base_dir = "output/subclusters",
  strategy = "ontology",
  cl = cl,
  tissue_name = "mouse heart",
  select_best = TRUE
)

# Marker inheritance strategy
workflow_results <- run_subcluster_annotation_workflow(
  base_dir = "output/subclusters",
  strategy = "marker_inheritance",
  cl = cl,  # Required for ontology distance calculation
  parent_marker_root = "output/markers",
  parent_res = "0.3",
  parent_cluster_col = "cluster_res.0.3",
  tissue_name = "mouse heart"
)
}
}
