% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subcluster_anno.R
\name{annotate_subclusters}
\alias{annotate_subclusters}
\title{Annotate Subclusters with Unified Strategy}
\usage{
annotate_subclusters(
  seurat_obj,
  strategy_config,
  resolutions,
  marker_dir,
  model = "gpt-4o",
  tissue_name = NULL,
  n_runs = 2,
  topgenenumber = 10,
  add_cl_prompt = FALSE,
  ontology_graph = NULL,
  subcluster_prefix = "subcluster_res.",
  original_cluster_col = NULL,
  save_dir = NULL,
  save_plots = TRUE,
  llm_config = NULL
)
}
\arguments{
\item{seurat_obj}{Subcluster Seurat object}

\item{strategy_config}{List returned by prepare_*_strategy() functions}

\item{resolutions}{Numeric vector of resolutions to annotate}

\item{marker_dir}{Directory containing marker RDS files}

\item{model}{Character. GPT model (default: "gpt-5")}

\item{tissue_name}{Character. Base tissue context}

\item{n_runs}{Integer. Number of GPT runs to aggregate (default: 2)}

\item{topgenenumber}{Integer. Number of top marker genes per cluster (default: 10)}

\item{add_cl_prompt}{Logical. Add Cell Ontology prompt (default: FALSE)}

\item{ontology_graph}{Optional igraph for distance calculation. If provided, avg_distance
will be calculated for use in scoring. Pass the result of build_ontology_graph(cl)}

\item{subcluster_prefix}{Character. Metadata column prefix (default: "subcluster_res.")}

\item{original_cluster_col}{Character. Column for parent cluster assignment (needed for marker_inheritance)}

\item{save_dir}{Optional directory to save results}

\item{save_plots}{Logical. Save comparison plots? (default: TRUE)}

\item{llm_config}{Optional list. LLM configuration forwarded to \code{summarize_gptcelltype()}.}
}
\value{
List with annotation results per resolution
}
\description{
Core annotation function that works for both ontology and marker-inheritance strategies.
Uses gptcelltype() and summarize_gptcelltype() from cluster_anno.R.
}
