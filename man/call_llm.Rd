% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm.R
\name{call_llm}
\alias{call_llm}
\title{Call LLM API}
\usage{
call_llm(
  prompt,
  provider = "openai",
  model = NULL,
  params = NULL,
  api_key = NULL,
  system_prompt = NULL,
  api_url = NULL
)
}
\arguments{
\item{prompt}{Character. The prompt to send.}

\item{provider}{Character. One of "openai", "anthropic", "gemini", "ollama", or "vllm".}

\item{model}{Character. Model name (e.g., "gpt-5.2", "claude-opus-4-6", "gemini-2.0-flash").
For Ollama, use model names like "llama2", "mistral", "neural-chat", etc.}

\item{params}{An ellmer params object created by ellmer::params().}

\item{api_key}{Character. API key (uses env var if NULL).
OpenAI: OPENAI_API_KEY
Anthropic: ANTHROPIC_API_KEY
Gemini: GOOGLE_API_KEY or GEMINI_API_KEY
Ollama/vLLM: Not required (local)}

\item{system_prompt}{Character. System prompt.}

\item{api_url}{Character. API URL for local LLMs (Ollama or vLLM).
Ollama default: "http://localhost:11434"
vLLM default: "http://localhost:8000"}
}
\value{
Character. The LLM response text.
}
\description{
Routes prompts to OpenAI, Anthropic, Google Gemini, or local Ollama models via ellmer package.
}
\details{
\strong{Ollama Setup:}
To use Ollama locally, first install it from https://ollama.ai, then run:

\if{html}{\out{<div class="sourceCode">}}\preformatted{ollama pull llama2       # or another model
ollama serve             # start the server (runs on localhost:11434)
}\if{html}{\out{</div>}}

Popular models: llama2, mistral, neural-chat, dolphin-mixtral, etc.

\strong{vLLM Setup:}
Similar to Ollama but optimized for performance. Requires Python installation.
}
